{
  "name": "botdi",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck": "tsc -b",
    "dev": "vite"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
